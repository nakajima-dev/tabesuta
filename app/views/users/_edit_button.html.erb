<% if user == current_user %>
  <%= link_to 'プロフィール編集', edit_user_path(user), class: "btn btn-primary btn-block mt-4" %>
  <%= link_to '退会する', user, method: :delete, data: {confirm: '本当に退会しますか？'}, class: "btn btn-danger btn-block mtb-2" %>
<% end %>